# Configuración para producción
PORT=3001
NODE_ENV=production

# Base de datos (Railway te dará esta URL)
DATABASE_URL=${DATABASE_URL}

# OpenAI (configurar en variables de entorno de Railway)
OPENAI_API_KEY=${OPENAI_API_KEY}
